当前每个模型通过api调用时都需要使用完整名字。需要让api调用时支持别名，通过在.env中指定每个模型的别名来调用相应模型。此外，分析当前/models如何获取模型列表，相关的实现是否完整。


分析当前的批处理配置是否在处理小批量、分散的任务时存在问题。例如当前仅通过单个时间间隔+最小批大小来决定是否在没有足够任务时提前批处理，但并未给出最终的降级方案。需要在现有等待时间的基础上额外添加一个超时机制，用于在挂起较长时间(如1s)后直接触发执行所有剩余任务(只有剩余任务大于0就执行)，避免少量任务时长期挂起。